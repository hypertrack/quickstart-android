<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:icon="@drawable/ic_green"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning">
        <activity
            android:name="com.hypertrack.quickstart.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="HyperTrackPublishableKey"
            android:value="Paste_your_publishable_key_here" />

        <!-- You can check the description of the SDK config params in the docs:
            https://hypertrack.com/docs/sdk-config -->
        <meta-data
            android:name="HyperTrackAllowMockLocation"
            android:value="true" />
        <meta-data
            android:name="HyperTrackForegroundNotificationTitle"
            android:resource="@string/notification_title" />
        <meta-data
            android:name="HyperTrackForegroundNotificationText"
            android:resource="@string/notification_message" />

    </application>

</manifest>
